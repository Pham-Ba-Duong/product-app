<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        <%= title || 'Product App' %>
    </title>
    <link rel="stylesheet" href="/public/styles.css">
</head>

<body>
    <header>
        <nav>
            <a href="/">Home</a> |
            <% if (user) { %>
                <span>Xin chào, <%= user.username %></span>
                <form method="post" action="/auth/logout" style="display:inline"><button type="submit">Logout</button>
                </form>
                <% } else { %>
                    <a href="/auth/login">Login</a> |
                    <a href="/auth/register">Register</a>
                    <% } %>
        </nav>
    </header>
    <main>
        <%- body %>
    </main>
</body>

</html>